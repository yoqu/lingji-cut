# 模型卡片资产中心与 AI 物品生成规范

> 状态：草案  
> 日期：2026-07-08  
> 适用范围：灵机剪影的 Motion Card、封面、时间线素材、AI 视频流水线  
> 目标：把一次性生成的卡片画面，升级为可沉淀、可复用、可控风格的视觉资产系统。

---

## 一、背景与问题

当前 Motion Card 主要依赖模型在卡片代码中直接绘制或用 prompt 临场描述视觉元素。这对数字、文字、图表、简单图形比较有效，但对具有明确特征的物品表现不稳定，例如磁带机、印章、旧报纸、手写便签、药瓶、手机、票据、人物剪影、证据板、设备零件等。

直接让模型在卡片里“画物品”会带来三个问题：

1. **识别度不稳定**：复杂物品容易变形、缺少关键特征，且不同卡片之间风格漂移。
2. **合成感不可控**：写实图、插画、3D、扁平图标混用时，整体容易像临时拼贴，降低成片质感。
3. **资产无法沉淀**：每个项目、每张卡都重新生成，无法复用创作者长期积累的视觉素材。

因此需要新增一个跨项目复用的资产模块，并把 AI 物品生成、抠图、标准化处理、卡片放置纳入 Motion Card 生成链路。

## 二、目标

### 2.1 产品目标

- 建立一个**跨项目复用的全局资产中心**，支持用户长期收集、整理和复用图片、视频、透明物件、绿幕素材、背景、纹理等素材。
- 建立**项目资产池**，记录当前项目实际使用的素材，支持引用全局资产或复制快照到项目目录。
- 让 Motion Card 导演阶段能够规划需要的物品资产，并在生成卡片前完成素材检索、生成、抠图和标准化。
- 提升模型卡片画面的识别度、一致性和栏目化质感。

### 2.2 工程目标

- 资产信息结构化：文件与元数据分离，支持搜索、标签、来源、版权、风格、适配场景等字段。
- 生成流程可观测：AI 物品生成、抠图、入库、引用需要进入统一任务进度与日志体系。
- 输出可回放：项目文件能稳定引用资产，项目迁移或归档时能检查缺失素材。
- 渐进接入：第一阶段不重写 Motion Card 架构，只增加资产选择与引用能力。

## 三、非目标

- 不在第一版实现完整 DAM（Digital Asset Management）系统。
- 不在第一版自动判断所有素材版权风险，只记录来源与备注。
- 不强制所有卡片都使用资产。抽象图形、数据图表、简单背景仍由 Motion Card 直接生成。
- 不把 AI 生成图片作为唯一来源。用户手动收集、导入、整理的素材是同等重要的一等来源。
- 不允许资产模块破坏当前 macOS 专业工具风格：不做花哨素材市场、不做营销式素材墙。

## 四、核心原则

### 4.1 资产驱动，而不是提示词驱动

有特征的物品应尽量资产化。导演模型负责判断“这段内容需要什么视觉锚点”，资产系统负责提供稳定素材，卡片系统负责布局、运动和合成。

### 4.2 写实是原料，设计语言是成品标准

主材可以偏写实，以保留物品识别度；但写实素材不能直接裸贴进卡片。所有主材必须经过统一的视觉处理，符合当前 Motion Card 的设计语言。

推荐默认风格为：

```text
Editorial Realist Cutout

真实物品
  + 干净抠图
  + 低饱和统一调色
  + 克制阴影
  + 一致光照方向
  + 卡片系统的排版、图形标注和动画
```

物品负责“可信和具体”，卡片系统负责“高级和统一”。

### 4.3 项目稳定性优先

跨项目复用很重要，但项目可打开、可迁移、可归档更重要。因此资产引用需要支持两种模式：

- **引用全局资产**：节省空间，适合本机长期复用。
- **复制到项目资产**：保证项目独立完整，适合归档、迁移、交付。

### 4.4 低摩擦导入，高质量复用

用户可以快速拖入素材，但系统应在后台补齐元数据、缩略图、透明通道检测、尺寸检测、哈希去重等工作。

## 五、概念模型

### 5.1 全局资产库

全局资产库是跨项目共享的素材目录，默认由应用设置指定。

建议默认位置：

```text
~/Movies/灵机剪影/Assets/
```

结构草案：

```text
Assets/
  library.json
  originals/
    images/
    videos/
    audio/
  processed/
    cutouts/
    thumbnails/
    proxies/
    masks/
  generated/
    batches/
    objects/
  imports/
    2026-07/
```

### 5.2 项目资产池

项目资产池记录当前项目实际用到的素材和引用关系。

建议项目内结构：

```text
project/
  assets/
    manifest.json
    imported/
    generated/
    proxies/
    thumbnails/
```

项目资产池可以保存：

- 全局资产引用
- 项目本地素材
- AI 生成素材
- 卡片实际使用的裁切、遮罩、代理文件

### 5.3 资产记录

资产记录是资产系统的一等对象，不等于文件本身。

```jsonc
{
  "id": "asset_vintage_tape_recorder_001",
  "name": "老式磁带录音机",
  "kind": "image",
  "role": "object",
  "sourceType": "manual-import",
  "sourceUri": "",
  "licenseNote": "个人收集，待确认商用权限",
  "files": {
    "original": "originals/images/asset_vintage_tape_recorder_001.png",
    "processed": "processed/cutouts/asset_vintage_tape_recorder_001.png",
    "thumbnail": "processed/thumbnails/asset_vintage_tape_recorder_001.webp",
    "mask": "processed/masks/asset_vintage_tape_recorder_001.png"
  },
  "metadata": {
    "width": 2048,
    "height": 2048,
    "durationMs": null,
    "hasAlpha": true,
    "dominantColors": ["#5d554a", "#b8a27b"],
    "contentHash": "sha256:..."
  },
  "semantic": {
    "tags": ["复古", "声音", "采访", "设备"],
    "topics": ["媒体", "录音", "怀旧"],
    "style": ["写实", "低饱和", "桌面道具"],
    "usableAs": ["foreground-object", "detail-prop"]
  },
  "treatment": {
    "profile": "editorial-realist-cutout",
    "lighting": "soft-left",
    "palette": "low-saturation",
    "shadow": "soft-ground",
    "perspective": "front-3q"
  },
  "usage": {
    "lastUsedAt": "2026-07-08T10:00:00+08:00",
    "projectRefs": [],
    "favorite": false
  }
}
```

## 六、资产类型

| 类型 | role | 说明 | 典型用途 |
|---|---|---|---|
| 图片物件 | `object` | 抠图后的单个物品 | 卡片主视觉、辅助道具 |
| 背景图 | `background` | 场景、纹理、环境底图 | 卡片背景、封面背景 |
| 纹理 | `texture` | 纸张、噪声、胶片颗粒 | 统一视觉质感 |
| 图标 / 标志 | `symbol` | 简洁标记、品牌化符号 | 信息标注、流程节点 |
| 视频素材 | `video` | 可直接进时间线的视频 | 背景循环、真实画面 |
| 绿幕视频 | `greenscreen-video` | 需要抠像的视频 | 人物、动作、特效 |
| 透明叠层 | `overlay` | alpha 通道视频或图片 | 光效、框线、扫描线 |
| 生成批次 | `generation-batch` | 一次 AI 生成的原始结果集合 | 后续挑选、抠图、入库 |

## 七、主材风格策略

### 7.1 默认策略

默认不要求素材本体完全等同于当前 UI 设计语言。素材本体可以写实，但进入卡片前必须经过统一处理：

- 色温统一：默认轻微冷静、低饱和，避免跳出暗色专业工具感。
- 对比度统一：减少过硬阴影与过曝高光。
- 光照统一：默认左上或左侧柔光。
- 阴影统一：由卡片运行时生成软接触阴影，不依赖原图杂乱阴影。
- 透视统一：优先正面、三分之四侧面、轻俯视三类，避免同一卡片透视冲突。
- 边缘统一：抠图边缘干净，禁止绿边、白边、锯齿边。

### 7.2 风格档案

第一版建议只内置一个默认处理档案：

```text
editorial-realist-cutout
```

后续可以扩展：

- `documentary-desk`：纪录片桌面道具，适合历史、财经、知识类内容。
- `technical-product`：产品摄影质感，适合科技、设备、工具。
- `paper-archive`：纸本档案、报纸、票据类素材。
- `diagram-prop`：介于真实物件与图解之间的半抽象道具。

### 7.3 反模式

以下情况会让画面显得像学生作品，应通过检测或人工确认避免：

- 同一张卡混用写实摄影、3D 卡通、扁平图标、厚涂插画。
- 物品自带强烈光源或投影，和卡片背景冲突。
- 素材边缘残留绿幕、白边或过度羽化。
- 素材分辨率过低，被放大后发糊。
- 同一项目中反复出现风格不一致的同类物品。
- 主材过于抢眼，压过字幕、数字、图表和论点。

## 八、AI 物品生成流程

### 8.1 导演规划

Motion Card 导演阶段新增 `assets` 字段，用于描述本段卡片需要的物品资产。

```jsonc
{
  "claim": "这一段的核心论点",
  "carrier": "concept",
  "scene": "一张旧桌面上，磁带机与手写便签作为采访记忆的视觉锚点",
  "assets": [
    {
      "slot": "main-prop",
      "query": "老式磁带录音机",
      "role": "object",
      "importance": "primary",
      "reusePolicy": "prefer-library",
      "visualTreatment": "editorial-realist-cutout",
      "placementHint": "画面左下，低饱和，作为主视觉道具"
    },
    {
      "slot": "detail-prop",
      "query": "泛黄手写便签",
      "role": "object",
      "importance": "secondary",
      "reusePolicy": "generate-if-missing",
      "visualTreatment": "paper-archive",
      "placementHint": "画面右侧，承载关键词标注"
    }
  ]
}
```

### 8.2 资产解析器

导演输出后进入资产解析器：

1. 读取 `assets` 需求。
2. 在当前项目资产池搜索。
3. 在全局资产库搜索。
4. 如果找到高匹配资产，返回候选。
5. 如果缺失，根据 `reusePolicy` 决定是否触发 AI 生成。
6. 用户可在检查面板中替换、固定或拒绝候选资产。

### 8.3 集中生成

缺失物品较多时，优先集中在一张生成图中完成：

```text
一张绿色背景的素材表，包含 4 个彼此分离的物品：
1. 老式磁带录音机
2. 泛黄手写便签
3. 黑色签字笔
4. 小型录音麦克风

要求：每个物品完整、互不遮挡、边缘清晰、低饱和写实摄影风格、无文字水印。
```

集中生成的优点：

- 减少多次图片调用成本。
- 同一批素材更容易保持风格一致。
- 适合多个道具同属一张卡或同一项目主题。

限制：

- 透明、玻璃、烟雾、毛发、复杂网格不适合绿幕批量抠图。
- 高价值主视觉建议单物品生成，保证构图与细节。
- 物品之间需要足够间距，避免抠图时互相污染。

### 8.4 抠图与标准化

生成结果进入处理管线：

```text
原始生成图
  -> 物品分割
  -> 绿幕抠除 / 背景移除
  -> 边缘修正
  -> 颜色归一
  -> 阴影移除或弱化
  -> 透明 PNG / WebP 输出
  -> 缩略图与元数据生成
  -> 待确认入库
```

第一版可以先实现半自动：

- 自动生成处理结果。
- 用户在“待确认入库”中选择保留、重命名、打标签、丢弃。
- 被使用到卡片中的素材自动进入项目资产池。

### 8.5 卡片放置

卡片生成阶段不再让模型直接绘制物品，而是引用资产：

```jsonc
{
  "slot": "main-prop",
  "assetId": "asset_vintage_tape_recorder_001",
  "placement": {
    "x": 96,
    "y": 392,
    "width": 260,
    "rotation": -2,
    "opacity": 0.92,
    "depth": "foreground"
  },
  "motion": {
    "enter": "fade-up-soft",
    "emphasis": "subtle-parallax",
    "exit": "hold"
  }
}
```

最终由 Motion Card runtime 统一负责阴影、调色、轻微景深、遮罩和安全区限制。

## 九、页面与交互设计

### 9.1 入口

建议新增顶层或侧栏入口：

```text
资产
```

也可在视频工作台中作为素材面板的一个 tab：

```text
素材 / 资产中心 / 当前项目
```

第一版优先做工作台内入口，降低导航复杂度；后续再升级为独立资产中心页面。

### 9.2 全局资产库页面

核心区域：

- 左侧：分类、标签、来源、收藏、最近使用。
- 中间：缩略图网格 / 列表视图。
- 右侧：资产详情 Inspector。

关键操作：

- 导入文件或文件夹。
- 搜索资产。
- 编辑名称、标签、来源、版权备注。
- 查看原图、处理图、缩略图。
- 加入当前项目。
- 标记收藏。
- 重新处理抠图或生成代理文件。

### 9.3 当前项目资产页面

核心分组：

- 已使用：被时间线、卡片、封面引用。
- 未使用：导入但未放入作品。
- 缺失：路径失效或全局引用不可访问。
- 待归档：建议复制到项目目录的全局引用。

关键操作：

- 定位使用位置。
- 替换素材。
- 复制全局素材到项目。
- 清理未使用素材。
- 检查项目完整性。

### 9.4 AI 生成任务页面

面向模型卡片生成流程中的资产任务。

状态分组：

- 待生成。
- 生成中。
- 待抠图。
- 待确认入库。
- 已用于卡片。
- 失败 / 需重试。

用户需要能做三件事：

- 看懂为什么生成这些物品。
- 快速接受或替换结果。
- 把好素材沉淀到全局库。

### 9.5 资产详情 Inspector

字段：

- 预览。
- 名称。
- 类型。
- 标签。
- 来源。
- 版权备注。
- 风格处理档案。
- 文件信息。
- 使用记录。
- 操作按钮：加入项目、替换、重新处理、复制路径、删除引用。

UI 风格应遵循 `.impeccable.md`：

- 暗色、低对比、专业工具质感。
- 不做花哨卡片墙。
- 缩略图是内容主角，UI 控件克制。
- 系统蓝仅用于明确主操作。

## 十、Motion Card 接入

### 10.1 Storyboard Schema 扩展

新增可选字段：

```ts
type StoryboardAssetRequest = {
  slot: string;
  query: string;
  role: 'object' | 'background' | 'texture' | 'symbol' | 'overlay';
  importance: 'primary' | 'secondary' | 'ambient';
  reusePolicy: 'prefer-library' | 'generate-if-missing' | 'always-generate' | 'manual-only';
  visualTreatment: string;
  placementHint?: string;
  negativePrompt?: string;
};
```

### 10.2 Render Plan 扩展

卡片渲染计划新增资产绑定：

```ts
type CardAssetBinding = {
  slot: string;
  assetId: string;
  filePath: string;
  treatment: AssetTreatment;
  placement: AssetPlacement;
};
```

### 10.3 Lint 与质检

新增检查项：

- assetId 是否存在。
- filePath 是否可读。
- 资产是否在项目归档模式下可迁移。
- 主视觉是否侵入字幕安全区。
- 资产尺寸是否低于建议阈值。
- 同一卡片是否混用冲突的 `treatment.profile`。

### 10.4 降级策略

当资产缺失或生成失败：

- primary asset 缺失：卡片生成应提示用户或回退到抽象表达，不应静默生成错误物品。
- secondary asset 缺失：可使用图形符号或纹理替代。
- ambient asset 缺失：直接跳过。

## 十一、存储与文件策略

### 11.1 全局库

全局库记录在应用设置中：

```jsonc
{
  "assetLibrary": {
    "rootDir": "~/Movies/灵机剪影/Assets",
    "defaultImportMode": "copy",
    "defaultProjectReferenceMode": "reference-global"
  }
}
```

### 11.2 项目引用

项目 manifest 中记录引用：

```jsonc
{
  "assetRefs": [
    {
      "assetId": "asset_vintage_tape_recorder_001",
      "scope": "global",
      "globalLibraryId": "default",
      "snapshotPath": null,
      "usedBy": [
        {
          "type": "motion-card",
          "cardId": "card_001",
          "slot": "main-prop"
        }
      ]
    }
  ]
}
```

### 11.3 打包与迁移

项目导出或归档时提供检查：

- 全局引用是否需要复制到项目。
- 缺失文件列表。
- 外部路径素材列表。
- AI 生成但未确认入库的临时素材。

## 十二、任务进度与日志

资产生成属于耗时任务，应接入统一进度系统。

建议任务类型：

- `asset.import`
- `asset.process`
- `asset.generate`
- `asset.cutout`
- `asset.archive`

AI 生成相关事件应进入已有观测体系，至少记录：

- 资产需求数量。
- 命中素材库数量。
- 需要生成数量。
- 图片 provider、耗时、失败原因。
- 抠图耗时。
- 入库结果。

禁止为资产生成另建独立日志体系。

## 十三、MVP 范围

第一阶段只做最短闭环：

1. 全局资产目录配置。
2. 本地图片 / 视频导入。
3. 缩略图生成。
4. 基础元数据与标签。
5. 当前项目资产池。
6. Motion Card 可引用项目资产。
7. 导演阶段可输出 asset requests。
8. 资产解析器优先从项目与全局库匹配。
9. 缺失资产进入“待生成”列表。
10. AI 生成结果可以手动确认入库。

暂缓：

- 完整自动抠图批处理。
- 版权风险自动识别。
- 相似图片聚类。
- 云同步。
- 多用户协作。
- 素材市场。

## 十四、实施分期

### Phase 1：资产基础设施

- 定义资产数据结构。
- 建立全局库与项目资产池 manifest。
- 支持导入、缩略图、元数据、标签。
- 新增资产浏览 UI 与 Inspector。

验收：

- 用户能导入素材。
- 用户能在多个项目中看到同一全局素材。
- 项目能引用全局素材并显示缩略图。

### Phase 2：Motion Card 引用资产

- Storyboard 增加 `assets` 字段。
- 卡片生成流程增加资产解析步骤。
- Render Plan 支持 asset binding。
- 卡片 runtime 支持透明图片放置、阴影、调色与安全区约束。

验收：

- 一张卡可以稳定引用项目资产。
- 资产缺失时有明确提示。
- 卡片导出与预览引用同一资产文件。

### Phase 3：AI 生成与待确认入库

- 缺失资产生成任务。
- 支持批量绿幕生成提示词。
- 生成结果进入待确认列表。
- 用户可重命名、打标签、确认加入项目或全局库。

验收：

- 导演规划的物品缺失时能触发生成。
- 生成素材可以被当前卡片使用。
- 被确认的素材可跨项目复用。

### Phase 4：抠图与风格标准化

- 接入背景移除 / 绿幕抠除。
- 增加边缘修正、色彩归一、阴影策略。
- 增加 `visualTreatment` 档案。
- 检测同卡风格冲突。

验收：

- 生成物品能自动输出透明背景版本。
- 写实素材进入卡片后风格不突兀。
- 卡片主材不侵入字幕安全区。

### Phase 5：归档、去重与质量工具

- 内容哈希去重。
- 相似素材提示。
- 项目完整性检查。
- 一键复制全局引用到项目。
- 未使用素材清理。

验收：

- 项目可迁移到另一台机器并完整打开。
- 用户能定位缺失素材与使用位置。

## 十五、验收标准

整体功能达到可用状态时，应满足：

1. 用户可以建立跨项目复用的全局素材库。
2. 用户可以在项目中引用全局素材，也可以复制为项目本地素材。
3. Motion Card 导演能规划物品资产需求。
4. 资产解析器能优先复用已有素材。
5. 缺失物品能进入 AI 生成任务。
6. 生成素材能经过确认后进入项目或全局库。
7. 卡片可以稳定引用透明物件，并由 runtime 统一处理阴影、调色和安全区。
8. 项目归档时能识别缺失素材与外部引用。
9. UI 符合灵机剪影的暗色专业工具设计语言。
10. 写实主材不会破坏卡片整体风格一致性。

## 十六、开放问题

1. 全局资产库默认目录是否放在 `~/Movies/灵机剪影/Assets/`，还是跟随应用数据目录？
2. 项目默认使用“引用全局资产”还是“复制到项目资产”？
3. 第一版 AI 抠图使用本地算法、现有图片 provider，还是独立接入背景移除服务？
4. 素材来源与版权备注是否必须填写，还是仅对互联网导入素材提示填写？
5. Motion Card 是否需要在 Inspector 中提供“替换该卡片资产”的快捷入口？
6. 资产搜索第一版是否只做标签 / 名称搜索，还是同步做向量语义检索？

## 十七、推荐下一步

建议先做 Phase 1 + Phase 2 的技术设计，不急于实现全自动生成。原因是资产中心的核心价值是“可沉淀、可复用、可稳定引用”。只要先把资产记录、项目引用和 Motion Card asset binding 打通，后续 AI 生成、抠图和风格标准化都可以自然接入。

第一版实现应优先保证：

- 文件不丢。
- 引用可追踪。
- UI 不打断批量工作节奏。
- 卡片能稳定使用同一份资产。
- 素材处理结果能被人工确认，而不是完全黑盒自动化。
